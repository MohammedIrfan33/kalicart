// ignore_for_file: public_member_api_docs, sort_constructors_first
class CategoryItem {
  String ? header;
  String ? data;
  bool  ? isExpanded;
  CategoryItem({
    this.header,
    this.data,
    this.isExpanded,
  });
}
